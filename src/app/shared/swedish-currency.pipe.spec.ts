import { SwedishCurrencyPipe } from './swedish-currency.pipe';

describe('SwedishCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SwedishCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
